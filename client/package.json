{
  "name": "client",
  "version": "1.0.0",
  "description": "",
  "main": "scripts.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "parcel index.html",
    "parcel": "npm install -g parcel-bundler",
    "build": "npm install autoprefixer && parcel build index.html"
  },
  "proxy": "http://localhost:8080",
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "node-sass": "^7.0.0"
  },
  "browserslist": [
    "last 1 Chrome version"
  ]
}
